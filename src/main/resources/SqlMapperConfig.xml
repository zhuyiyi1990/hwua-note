<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--引用外部属性文件信息-->
    <properties resource="jdbcConfig.properties"/>

    <!--配置延迟加载参数-->
    <settings>
        <setting name="lazyLoadingEnabled" value="true"/>
        <setting name="aggressiveLazyLoading" value="false"/>
        <setting name="cacheEnabled" value="true"/>
    </settings>

    <!--自定义别名-->
    <typeAliases>
        <!--单个别名定义,不区分大小写,如果多个类都要配置比较麻烦-->
        <!--<typeAlias type="com.hwua.pojo.User" alias="user"/>-->
        <!--批量别名定义,指定后当前包下的类都会注册别名,且类名就是别名,不区分大小写-->
        <package name="com.github.zhuyiyi1990.pojo"/>
    </typeAliases>

    <!--配置mybatis的环境-->
    <environments default="mysql">
        <!--配置mysql的环境-->
        <environment id="mysql">
            <!--配置事务的类型,默认jdbc事务处理-->
            <transactionManager type="JDBC"/>
            <!--配置数据库连接的信息,用的是数据源(连接池)-->
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>

    <!--告知mybatis映射配置的位置-->
    <mappers>
        <package name="com.github.zhuyiyi1990.dao"/>
    </mappers>
</configuration>