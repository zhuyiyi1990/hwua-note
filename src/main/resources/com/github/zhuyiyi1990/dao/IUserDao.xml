<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace属性值必须为持久层接口的全限定类名,  命名空间+id来确定唯一的sql语句-->
<mapper namespace="com.github.zhuyiyi1990.dao.IUserDao">
    <!--开启二级缓存-->
    <cache/>

    <!-- 建立 User 实体和数据库表的对应关系
    id 属性：给定一个唯一标识，是给查询 select 标签引用用的。-->
    <resultMap id="userMap" type="com.github.zhuyiyi1990.pojo.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="birthday" property="birthday"/>
        <result column="sex" property="sex"/>
        <result column="address" property="address"/>
    </resultMap>

    <resultMap id="userAccountMap" type="com.github.zhuyiyi1990.pojo.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="birthday" property="birthday"/>
        <result column="sex" property="sex"/>
        <result column="address" property="address"/>
        <!--配置user中的accounts集合映射
    property指定属性 ofType指定集合中的元素类型-->
        <!--<collection property="accounts" ofType="account">
            <id column="aid" property="id"/>
            <result column="uid" property="uid"/>
            <result column="money" property="money"/>
        </collection>-->
        <collection property="accounts" ofType="account" column="id"
                    select="com.github.zhuyiyi1990.dao.IAccountDao.findByUid"/>
    </resultMap>

    <resultMap id="userRoleMap" type="com.github.zhuyiyi1990.pojo.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="birthday" property="birthday"/>
        <result column="sex" property="sex"/>
        <result column="address" property="address"/>
        <!--配置user中的accounts集合映射
    property指定属性 ofType指定集合中的元素类型-->
        <collection property="roles" ofType="role">
            <id column="rid" property="id"/>
            <result column="rname" property="roleName"/>
            <result column="money" property="roleDesc"/>
        </collection>
    </resultMap>

    <!-- 了解的内容:抽取重复的内容 -->
    <sql id="defaultSql">
        select * from user
    </sql>

    <!--配置查询所有操作-->
    <!-- id为方法名  resultType表示返回值类型 -->
    <!--<select id="findAll" resultMap="userMap">
        <include refid="defaultSql"></include>
    </select>-->

    <!--查找所有的用户,及对应的账户信息-->
    <select id="findAll" resultMap="userAccountMap">
        <!--select u.*,a.id aid,a.uid,a.money
        from user u left join account a
        on u.id=a.uid-->
        select * from user
    </select>

    <!--查找所有的用户,及对应的账户信息-->
    <!--<select id="findAll" resultMap="userRoleMap">
        select u.*,r.id rid,r.role_name rname,r.role_desc rdesc from user u
        left join user_role ur on u.id=ur.uid
        left join role r on r.id=ur.rid
    </select>-->

    <!--通过id查找用户-->
    <select id="findById" parameterType="int" resultType="com.github.zhuyiyi1990.pojo.User" useCache="true">
        <include refid="defaultSql"/>
        where id=#{id}
    </select>

    <!--保存用户-->
    <insert id="saveUser" parameterType="com.github.zhuyiyi1990.pojo.User">
        <selectKey keyProperty="id" keyColumn="id" order="AFTER" resultType="int">
            select last_insert_id();
        </selectKey>
        insert into user (username,birthday,sex,address)
        values (#{username},#{birthday},#{sex},#{address})
    </insert>

    <!--更新用户-->
    <update id="updateUser" parameterType="com.github.zhuyiyi1990.pojo.User">
        update user set username=#{username},birthday=#{birthday},sex=#{sex},address=#{address}
        where id=#{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from user where id=#{id}
    </delete>

    <!--通过名称模糊查找用户-->
    <select id="findByName" parameterType="String" resultType="com.github.zhuyiyi1990.pojo.User">
        <include refid="defaultSql"></include>
        where username like #{username}
    </select>

    <!--统计总记录条数-->
    <select id="findTotal" resultType="int">
        select count(id) from user
    </select>

    <!--根据用户名模糊查询用户信息，查询条件放到 QueryVo 的 user 属性中-->
    <select id="findByQueryVo" parameterType="com.github.zhuyiyi1990.pojo.QueryVo"
            resultType="com.github.zhuyiyi1990.pojo.User">
        <include refid="defaultSql"></include>
        where username like #{user.username}
    </select>

    <!--通过用户信息查询用户-->
    <select id="findByUser" parameterType="user" resultType="user">
        <include refid="defaultSql"></include>
        <where>
            <if test="username!=null and username!=''">
                and username=#{username}
            </if>
            <if test="sex!=null and sex!=''">
                and sex=#{sex}
            </if>
        </where>
    </select>

    <!--通过QueryVo中的id集合来查询用用户-->
    <select id="findByIds" parameterType="queryvo" resultType="user">
        <include refid="defaultSql"></include>
        <where>
            <if test="ids!=null and ids.size>0">
                <foreach collection="ids" open="id in (" close=")" item="id" separator=",">
                    #{id}
                </foreach>
            </if>
        </where>
    </select>
</mapper>