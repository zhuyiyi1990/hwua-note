<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace属性值必须为持久层接口的全限定类名,  命名空间+id来确定唯一的sql语句-->
<mapper namespace="com.github.zhuyiyi1990.dao.IUserDao">
    <!--配置查询所有操作-->
    <!-- id为方法名  resultType表示返回值类型 -->
    <select id="findAll" resultType="com.github.zhuyiyi1990.pojo.User">
        select * from user
    </select>

    <!--通过id查找用户-->
    <select id="findById" parameterType="int" resultType="com.github.zhuyiyi1990.pojo.User">
        select * from user where id=#{id}
    </select>
</mapper>